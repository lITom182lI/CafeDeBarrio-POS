# Contraseña del usuario SA de SQL Server para el contenedor Docker local.
# Copiar este archivo como .env y establecer el valor real antes de ejecutar docker-compose up.
MSSQL_SA_PASSWORD=your_strong_password_here
